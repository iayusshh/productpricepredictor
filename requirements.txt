# Build tools (MUST be first)
setuptools
wheel

# Web framework and deployment
flask==2.3.3
flask-cors==4.0.0
gunicorn==21.2.0

# Core data processing (minimal, Python 3.13 compatible)
numpy
pandas

# Machine learning (minimal)
scikit-learn
scipy

# NLP and embeddings
sentence-transformers
transformers

# Utilities
requests
Pillow
python-dotenv
